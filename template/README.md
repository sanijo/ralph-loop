# Ralph Loop Template Payload

Files in this directory are intended to be installed into target Git repositories by the Ralph Loop installer.

The initial payload is intentionally minimal. Later implementation slices will add the target `.ralph/` runner, prompt, provider bridges, helper scripts, and target repository documentation.
